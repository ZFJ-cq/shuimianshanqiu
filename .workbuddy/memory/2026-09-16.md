# 2026-09-16

## 睡眠债务山丘图（Sleep Debt Visualizer）MVP 搭建
- 技术栈：Next.js 14 (App Router) + TS + Tailwind v3 + 自写 SVG 图表 + localStorage，纯前端无后端。
- 功能：每日录入 0–12h（0.5 步进，7h 基准）；自定义 SVG 发散面积图（零轴之上绿=盈余、之下红=债务，悬停看每日明细）；近 7/30 天汇总（当前睡眠债 / 最长连亏天数 / 今日建议补觉 + 平均睡眠、记录完整度）；PDF 周报用 `window.print()`（无重型依赖）。
- 关键文件：`lib/sleep.ts`（持久化+债务纯函数）、`components/DebtHillChart.tsx`、`components/SleepEntryForm.tsx`、`components/StatsCards.tsx`、`app/page.tsx`。
- 构建：`npm run build` → `out/`（已配 `output:'export'`）。部署：`.github/workflows/deploy.yml` 走 GitHub Actions，base path 读 `vars.PAGES_BASE_PATH`。
- 坑：`next start` 在 `output:export` 下不可用，预览需静态服务 `out/`（如 `python3 -m http.server`）。

## 新增：睡眠打卡 / 早起打卡（11:08）
- 需求：用「入睡打卡 + 起床打卡」自动算出今日睡眠时长，替代手填小时。
- `lib/sleep.ts`：`SleepEntry` 增加可选 `bedtime`/`waketime`（"HH:MM"）；新增 `nowHM()`、`durationHours(bed, wake)`（跨天自动 +24h）；`upsertEntry` 增加 bedtime/waketime 参数；`loadEntries` 校验时间格式。
- `components/SleepEntryForm.tsx`：两个打卡按钮（记当前时间）+ 可编辑 time 输入；两个时间齐了自动算 hours（四舍五入 0.5h）并禁用滑块；滑块仍可手动覆盖。
- `app/page.tsx`：`handleSave` 透传时间；近期记录表增加「入睡/起床」列。
- 债务计算逻辑（buildRecords/summarize）完全复用，只认 hours，无改动。旧数据兼容（无时间则 null）。

## 打卡边界 + GitHub Pages 部署修复（14:45）
- 确认 01:00→08:00 = 7h 计算正确（waketime 分钟 > bedtime 分钟不触发跨午夜）；真正风险是 12/24h 制误填（13:00 当凌晨1点 → 19h）。
- `SleepEntryForm` 加 24h 制提示 + 超过 12h 的黄色误填告警（`isOver` 判定）。
- Pages 部署问题根因：Pages 发布源是「Deploy from a branch」，渲染了根目录 README（Jekyll），而非 Actions 的 `out/`；且 workflow 依赖手动设 `PAGES_BASE_PATH`。
- 修复：deploy.yml 自动用 `github.event.repository.name` 生成 BASE_PATH（vars.PAGES_BASE_PATH 可覆盖），构建后 `touch out/.nojekyll`；已推送 main（f1ce8f4）。**待用户在 Settings→Pages 把 Source 切为 GitHub Actions 并 Re-run**。
- 本地验证过 `BASE_PATH=/shuimianshanqiu npm run build`，out/index.html 资源路径带前缀正确。
